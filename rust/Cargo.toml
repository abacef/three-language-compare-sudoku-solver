[package]
name = "rust"
version = "0.1.0"
authors = ["bacef"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
regex = "1"
bit-set = "0"

[profile.release]
panic = "abort"
